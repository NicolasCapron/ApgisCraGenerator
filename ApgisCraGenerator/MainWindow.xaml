<Window x:Class="ApgisCraGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:ApgisCraGenerator.Views"
        mc:Ignorable="d"
        Title="APGIS CRA Generator" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <TabControl SelectedIndex="{Binding SelectedTabIndex}">
            <TabItem Header="📋 To-Do List">
                <views:TodoListView DataContext="{Binding TodoListViewModel}"/>
            </TabItem>
            <TabItem Header="📁 Projets">
                <views:ProjetView DataContext="{Binding ProjetViewModel}"/>
            </TabItem>
            <TabItem Header="📊 CRA">
                <views:CraView DataContext="{Binding CraViewModel}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>